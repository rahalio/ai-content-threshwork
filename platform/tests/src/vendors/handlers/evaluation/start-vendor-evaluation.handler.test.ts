/**
 * StartVendorEvaluation handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vendors
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { startVendorEvaluation } from "@ddd/api-server/domains/vendors/handlers";
import type { VendorsDDDDependencies } from "@ddd/api-server/domains/vendors";
import { createEvaluation as createEvaluationEntity } from "@ddd/factories/vendors";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ddd/tests/shared";
import { VENDORS_USE_CASES } from "../__factories__/use-cases.js";

describe("StartVendorEvaluation", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VendorsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VendorsDDDDependencies>(
      VENDORS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createEvaluationEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeStartVendorEvaluation, "execute").mockResolvedValue(useCaseResult);

    await startVendorEvaluation(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeStartVendorEvaluation.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
