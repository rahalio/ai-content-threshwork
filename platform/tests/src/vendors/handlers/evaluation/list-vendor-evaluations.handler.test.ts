/**
 * ListVendorEvaluations handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vendors
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listVendorEvaluations } from "@ddd/api-server/domains/vendors/handlers";
import type { VendorsDDDDependencies } from "@ddd/api-server/domains/vendors";
import { createEvaluation as createEvaluationEntity } from "@ddd/factories/vendors";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ddd/tests/shared";
import { VENDORS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListVendorEvaluations", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VendorsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VendorsDDDDependencies>(
      VENDORS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createEvaluationEntity({ orgId }),
      createEvaluationEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListVendorEvaluations, "execute").mockResolvedValue(useCaseResult);

    await listVendorEvaluations(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListVendorEvaluations.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
