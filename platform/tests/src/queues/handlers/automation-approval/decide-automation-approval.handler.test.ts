/**
 * DecideAutomationApproval handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: queues
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { decideAutomationApproval } from "@ddd/api-server/domains/queues/handlers";
import type { QueuesDDDDependencies } from "@ddd/api-server/domains/queues";
import { createAutomationApproval as createAutomationApprovalEntity } from "@ddd/factories/queues";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ddd/tests/shared";
import { QUEUES_USE_CASES } from "../__factories__/use-cases.js";

describe("DecideAutomationApproval", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: QueuesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<QueuesDDDDependencies>(
      QUEUES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createAutomationApprovalEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeDecideAutomationApproval, "execute").mockResolvedValue(useCaseResult);

    await decideAutomationApproval(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeDecideAutomationApproval.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
