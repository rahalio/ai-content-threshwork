/**
 * Portfolio DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: portfolio
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Portfolio Domain Module - Composition root
 *
 * DDD: Composition root for portfolio domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BenefitRepositoryAdapter,
  EgressRecordRepositoryAdapter,
  ExposureRepositoryAdapter,
  RescoreRepositoryAdapter,
  RoadmapRepositoryAdapter,
} from "@ddd/adapters/portfolio";
import { getIdGeneratorService } from "@ddd/adapters";
import type { AdapterDynamoDBClient } from "@ddd/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetBenefitRecord,
  ExecuteGetExposureLedger,
  ExecuteListEgressRecords,
  ExecuteListRoadmapEntries,
} from "@ddd/services/portfolio/usecases";
// Ports (interfaces)
import type {
  BenefitRepository,
  EgressRecordRepository,
  ExposureRepository,
  RescoreRepository,
  RoadmapRepository,
} from "@ddd/services/portfolio/ports";

export interface PortfolioDomainModule {
  repos: {
    benefits: BenefitRepository;
    egressRecords: EgressRecordRepository;
    exposures: ExposureRepository;
    rescores: RescoreRepository;
    roadmaps: RoadmapRepository;
  };
  useCases: {
    benefits: {
      get: ExecuteGetBenefitRecord;
    };
    egressRecords: {
      list: ExecuteListEgressRecords;
    };
    exposures: {
      get: ExecuteGetExposureLedger;
    };
    roadmaps: {
      list: ExecuteListRoadmapEntries;
    };
  };
}

export function buildPortfolioDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): PortfolioDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      benefits?: BenefitRepository;
      egressRecords?: EgressRecordRepository;
      exposures?: ExposureRepository;
      rescores?: RescoreRepository;
      roadmaps?: RoadmapRepository;
    } = {};

    obj.benefits = new BenefitRepositoryAdapter(dynamoClient);
    obj.egressRecords = new EgressRecordRepositoryAdapter(dynamoClient);
    obj.exposures = new ExposureRepositoryAdapter(dynamoClient);
    obj.rescores = new RescoreRepositoryAdapter(dynamoClient);
    obj.roadmaps = new RoadmapRepositoryAdapter(dynamoClient);
    return {
      benefits: obj.benefits!,
      egressRecords: obj.egressRecords!,
      exposures: obj.exposures!,
      rescores: obj.rescores!,
      roadmaps: obj.roadmaps!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    benefits: {
      get: new ExecuteGetBenefitRecord(executionContext, idGenerator, repos.benefits),
    },
    egressRecords: {
      list: new ExecuteListEgressRecords(executionContext, idGenerator, repos.egressRecords),
    },
    exposures: {
      get: new ExecuteGetExposureLedger(executionContext, idGenerator, repos.exposures),
    },
    roadmaps: {
      list: new ExecuteListRoadmapEntries(executionContext, idGenerator, repos.roadmaps),
    },
  };
  return { repos, useCases: useCases as PortfolioDomainModule["useCases"] };
}
